# 🚀 双变量挖掘与非参数统计分析平台 - 生产环境部署与运维手册 (V1.0)

本文档旨在详细说明《双变量关联挖掘与非参数统计分析平台》在生产环境下的完整部署方案、域名配置情况、核心接口细节以及后续的持续更新（CI/CD）运维指南。

---

## 🏗️ 整体部署架构概览

本平台采用完全的前后端分离架构，通过云端 Serverless 与云容器技术进行全球分发式部署：

- **前端 (Frontend)**：托管于 **Vercel** 边缘网络，提供极速的全球 CDN 加速访问。
- **后端 (Backend)**：容器化托管于 **Render**，作为持久化运转的 API 服务层。
- **代码仓库**: GitHub (`https://github.com/zsy-dream/BivaStat-Miner.git`)
- **主域名解析**: 阿里云 DNS 面板

### 🌐 域名及访问地址

目前系统已全部绑定自定义业务域名，并强制开启 HTTPS 传输加密：

| 模块 | 业务线 | 生产环境 URL | 内部提供商 |
|---|---|---|---|
| **前端站点 (Web)** | 用户端产品界面 | `https://stat-miner.zsypioneer.cn` | Vercel |
| **后端接口 (API)** | 核心算法微服务 | `https://api.stat-miner.zsypioneer.cn` | Render |

---

## 🛠️ 后端部署配置 (Render | Docker)

后端是一个完全基于 FastAPI 的 Python AI 微服务，使用 Docker 构建。

### 1. 自动构建机制 (Render)
后端服务已经与 GitHub 仓库的 `main` 分支绑定。当有新的后端代码提交到 `main` 时，Render 会自动触发 **Docker 重新构建并平滑重启**。

* **服务名**: `stat-miner-api`
* **运行环境**: Docker (通过根目录 `Dockerfile` 启动)
* **启动命令**: `uvicorn main:app --host 0.0.0.0 --port 8000` 
* **暴露端口**: FastAPI 监听在 `8000` 端口，Render 会自动将其映射到该域名的 `443/80`。

### 2. 后端环境变量
如果后续需要加装数据库、安全秘钥等，需要在 Render 的 **Environment Variables** 面板中添加。目前默认运行无需强依赖外部变量环境变量。

### 3. API 核心接口清单

目前前端访问后端的通信统一加上前缀 `https://api.stat-miner.zsypioneer.cn`。
以下为核心分析接口概要（详细业务逻辑已在 `main.py` 及路由内定义）：

| 功能 | 请求方式 | 接口路由 | 作用说明 |
|---|---|---|---|
| **存活探针** | `GET` | `/` | 验证 API 容器是否启动正常返回在线时长等。 |
| **数据上传与评估** | `POST` | `/api/data/upload` | 接收各类文件(CSV/Excel)进行质量评估。(对应前端数据管理) |
| **算法配置提交** | `POST` | `/api/algorithm/config` | 提交支持度/置信度/P值校验等启发式算法核心参数。 |
| **分析进度查询** | `GET` | `/api/algorithm/task_status` | 返回挖掘任务的执行百分比，用于界面进度条。 |
| **分析结果输出** | `GET` | `/api/analysis/results` | 返回全部挖掘出的关联网状图数据及报告参数。 |

> **运维提醒**：所有跨域（CORS）配置均已在 `main.py` 的 Middleware 中设为 `allow_origins=["*"]`（可接受所有的端请求）。对于生产加密级别较高的考虑，后续可在代码中将源限死为 `https://stat-miner.zsypioneer.cn`。

---

## 🎨 前端部署配置 (Vercel | React + Vite)

前端是一个基于 **React TypeScript** 和 **Vite** 构建的现代 Web 应用，UI 使用了 **TailwindCSS v3** 进行响应式适配。

### 1. 持续部署系统 (Vercel)
前端同样绑定了 GitHub `main` 分支。任何涉及 `frontend/` 目录的提交（包括我们刚改完所有的手机端适配、界面自适应），Vercel 都会自动进行 NPM 安装和二次打包。

* **项目名**: `biva-stat-miner`
* **根目录 (Root Directory)**: `frontend` (非常关键！因为仓库包含后端，Vercel 需切入子文件夹打包)。
* **框    架**: Vite
* **构建命令**: `npm run build` （内部指向 `tsc -b && vite build`，执行严格 TypeScript 检验）
* **输出目录**: `dist`

### 2. 生产环境变量
Vercel 控制台中配置了以下极其重要的变量（通过 `frontend/.env.production` 映射）：

* **`VITE_API_URL`**: `https://api.stat-miner.zsypioneer.cn`
> ⚠️ **注意**：前端代码内部不应再硬编码写死后端的本地地址（如 `localhost`）。所有 HTTP Axios 请求现统一从 `src/utils/api.ts` 中调用该环境变量，以此保证云端的联调成功匹配。

---

## 🔄 后续的运维与更新流程 (CI/CD)

整个项目的运维已经做到了**“零终端敲击化”**。当有新的需求、页面调整或是模型优化时，您的运维流程如下：

### 流程一：修改与验证代码
在本地电脑使用 VSCode 等工具进行调试：
1. 本地启动后端：`uvicorn main:app --reload`
2. 本地启动前端：`cd frontend && npm run dev`
3. 本地验证页面、API 和自适应样式是否满意。

### 流程二：一键推向全球
对代码满意后，打开终端，只需执行 Git 三件套：
```bash
git add .
git commit -m "feat(module): 更新了某某功能，并优化了手机界面"
git push origin main
```

### 流程三：自动化流水线监控
执行 Push 之后：
1. **渲染端**：Vercel（负责前端页面）和 Render（负责后台数据）会同时收到 GitHub 的代码变更推送（WebHook）。
2. **构建端**：它们会同时在两台云服务器上启动构建机，分别跑打包流程（Vercel 运行 npm build, Render 运行 docker build）。
3. **上线端**：通常在约 **1~2 分钟**内，两边自动更新缓存并重启服务。
4. **用户端**：客户刷新 `https://stat-miner.zsypioneer.cn` 即可享受最新系统，全程不掉丝滑。

---

## 🚨 常见问题与排错指南

如果线上突然崩了，请依次检查以下问题：

**1. 前端报错："Network Error" 或者数据一直在 Loading**
* **原因**：前端静态托管无恙，但和后端 Render 的链接断了。可能是因为免费版 Render 容器休眠，或者跨域拦截，或者 API 挂了。
* **排查方式**：直接在浏览器输入 `https://api.stat-miner.zsypioneer.cn/`。如果显示返回一段 JSON（类似 `{"status": "ok"}`），则代表后端没死。否则去 Render 后台看 Runtime Logs 日志。

**2. 前端推送代码后，Vercel 没起作用（构建失败）**
* **原因**：通常是 TypeScript 的**类型严格校验没过关**。我们在本地的浏览器运行能忽略某些小红下划线，但云端 `npm run build` 一旦遇到没用到的死变量、空值保护不够，就会直接熔断退出。或者像之前发生的，PostCSS 的配置文件因 Windows 隐形字符等因素引发系统间的不兼容。
* **排查方式**：登录 Vercel 控制台，点进红色的 `Build Failed` 日志，它将精确告诉你哪个文件的第几行报错。根据提示清理后，再次 `git push` 给它个触发指令即可。

**3. 在某些设备上排版错乱（移动端重叠等）**
* **原因**：Tailwind CSS 的响应式断点类（`md:`, `sm:` 等配置）被更改。
* **排查方式**：优先查看 `src/components/Layout.tsx` 的抽屉菜单代码，和 `src/pages/Home.tsx` 的 Grid 栅格体系配置。

*(End of Manual)*
